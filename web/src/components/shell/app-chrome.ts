/**
 * Shared top strip height for the sidebar rail header and the main column header
 * so their bottom borders align (see AppShell aside + ChatPage title row).
 */
export const APP_TOP_HEADER_BAR_CLASS = 'h-14 shrink-0 items-center';
